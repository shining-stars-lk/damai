#服务端口
server:
  port: 5082
# 应用名称
spring:
  application:
    name: provider-service
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: www.cookframe.com:8848