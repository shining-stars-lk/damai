#服务端口
server:
  port: 6085
# 应用名称
spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      discovery:
        server-addr: www.cookframe.com:8848
      config:
        server-addr: www.cookframe.com:8848
        file-extension: yaml