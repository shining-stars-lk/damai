#服务端口
server:
  port: 11182
# 应用名称
spring:
  application:
    name: ${prefix.distinction.name:damai}-job-service
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: www.cookframe.com:8848
      config:
        server-addr: 62.234.31.217:8848
        file-extension: yaml
        shared-configs:
          - data-id: common.yaml
            refresh: true
        username: nacos
        password: nacos
